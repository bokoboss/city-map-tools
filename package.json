{
  "name": "city-map-tools",
  "version": "2.2.0",
  "private": true,
  "description": "Static-first browser map annotation prototype with explicit analytical capability quarantine",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}

# R0-safe prototype — reference only

`r0-safe-prototype.html` is the unchanged production `index.html` from accepted
main `37417ff12c8adb5ec2de83da99aa3fdae720c09c` (R0B + CI Phase A).
It is preserved for the bounded migrations in #19–#21, not a production entry.
Its CDN dependencies and prototype editor behavior are isolated here. Vite builds
only the new root entry and does not include this reference in `dist/`.

Analytical quarantine and provider limitations still apply to this reference.
